package com.signaldesk.domain.enums;

/** The time horizon of a briefing's read — each is scored against its own price window. */
public enum Horizon {
    SHORT,   // days
    SWING,   // weeks
    LONG     // months
}
